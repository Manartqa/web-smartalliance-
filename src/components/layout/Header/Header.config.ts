/**
 * The header CTA is a link, not a `BaseLinkButton`: the design gives it a
 * tighter box (px-5 py-3.5, 13px) than the shared button scale.
 */
export const HEADER_CONTACT_CTA_CLASS =
  "inline-flex items-center gap-2.5 rounded-[10px] bg-yellow px-5 py-3.5 " +
  "text-[13px] font-semibold uppercase leading-none tracking-[0.4px] " +
  "text-[#0a0f1a] transition-colors hover:bg-[#e6b000]";
